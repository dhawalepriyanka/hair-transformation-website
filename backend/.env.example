PORT=5000
NODE_ENV=development
DATABASE_URL=postgresql://postgres:password@localhost:5432/dipali_clinic
JWT_SECRET=replace-with-a-long-random-secret
CORS_ORIGINS=http://localhost:3000

# Development fallback credentials used only when PostgreSQL is unavailable.
ADMIN_USERNAME=admin
ADMIN_PASSWORD=replace-this-password
RECEPTIONIST_USERNAME=receptionist
RECEPTIONIST_PASSWORD=replace-this-password
